export interface user{
    user?: string | null,
    password?: string | null,
    name?: string | null,
    lastname?: string | null
}

export interface sigin{
    user: string,
    password: string
}